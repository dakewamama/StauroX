# StauroX Configuration

# RPC Endpoints (comma-separated)
RPC_ENDPOINTS=https://api.mainnet-beta.solana.com,https://rpc.ankr.com/solana,https://solana-api.projectserum.com

# Consensus threshold (minimum RPCs that must agree)
VERIFICATION_THRESHOLD=3

# Health check interval (milliseconds)
HEALTH_CHECK_INTERVAL_MS=400

# API Configuration
API_PORT=8081
WEBSOCKET_PORT=8080

# Logging
RUST_LOG=info
